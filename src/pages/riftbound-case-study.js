import { RIFTBOUND_ASSETS } from "../data/assets.js";

function riftboundImage(src, alt, className = "") {
  return `<figure class="riftbound-screen ${className}"><img src="${src}" alt="${alt}" /><figcaption>${alt}</figcaption></figure>`;
}

export function riftboundCaseSections() {
  return `<div class="case-body riftbound-case-body">
    <section class="case-section"><h2>The challenge</h2><div><p>Riftbound brings together several kinds of information: navigation, character details, equipment, rewards, and battle controls. The challenge was to organize these elements clearly while maintaining the personality of a pixel RPG.</p><p>The interface needed to feel connected across screens, with enough visual detail to establish the game’s atmosphere while keeping important actions easy to find.</p></div></section>
    <section class="case-section"><h2>Mapping the experience</h2><div><p>The experience centers on three key screens: the lobby, character screen, and battle HUD.</p><p>The lobby introduces the game’s main destinations. The character screen focuses on inspecting a hero and their equipment. The battle HUD brings movement controls and abilities into the gameplay view.</p><p>Planning these screens helped establish what should receive attention in each context and which visual patterns should remain consistent throughout the interface.</p><div class="riftbound-gallery riftbound-gallery-map">${riftboundImage(RIFTBOUND_ASSETS.home, "Riftbound lobby screen with the game’s main destinations")}${riftboundImage(RIFTBOUND_ASSETS.heroes, "Riftbound character screen focused on the selected hero")}${riftboundImage(RIFTBOUND_ASSETS.battle, "Riftbound battle HUD with movement controls and abilities")}</div></div></section>
    <section class="case-section"><h2>Design decisions</h2><div><div class="design-decision riftbound-decision"><div class="riftbound-image-frame">${riftboundImage(RIFTBOUND_ASSETS.home, "Riftbound lobby interface")}</div><div><h3>Different priorities, one visual language</h3><p>Each screen serves a different purpose. The lobby emphasizes navigation, the character screen gives more space to the selected hero, and the battle HUD prioritizes controls and visibility.</p><p>Shared panel treatments, pixel-style icons, and consistent colors connect these layouts. This allows the composition to change with the player’s task while keeping the interface recognizable.</p></div></div><div class="design-decision riftbound-decision"><div class="riftbound-image-frame">${riftboundImage(RIFTBOUND_ASSETS.equipments, "Riftbound equipment screen with structured item information")}</div><div><h3>Balancing pixel art and readability</h3><p>Pixel art establishes Riftbound’s identity, but labels, values, and controls still need to be readable at smaller sizes.</p><p>The design uses visual hierarchy to separate primary actions from supporting information. Character artwork provides emphasis, while structured panels organize equipment and other details.</p></div></div></div></section>
    <section class="case-section"><h2>The interface</h2><div><p>These screens explore the lobby, character management, equipment, and battle HUD as parts of one connected player experience.</p><div class="riftbound-gallery riftbound-gallery-interface">${riftboundImage(RIFTBOUND_ASSETS.home, "Riftbound lobby")}${riftboundImage(RIFTBOUND_ASSETS.heroes, "Riftbound character screen")}${riftboundImage(RIFTBOUND_ASSETS.equipments, "Riftbound equipment screen")}${riftboundImage(RIFTBOUND_ASSETS.battle, "Riftbound battle HUD")}</div></div></section>
    <section class="case-section"><h2>What I learned</h2><div><p>This project helped me explore how visual identity and usability influence each other in game interfaces. Small choices in icon detail, typography, and spacing can affect how easily a screen can be understood.</p><p>It also highlighted the importance of evaluating controls in context. These screens demonstrate the visual concept, but their usability during actual gameplay still needs validation.</p><p>My next step would be to build an interactive prototype and test navigation, icon recognition, and control placement with players.</p></div></section>
  </div>`;
}

export function riftboundCover() {
  return riftboundImage(RIFTBOUND_ASSETS.hero, "Riftbound pixel RPG interface concept hero artwork", "riftbound-cover-image");
}

import { GCS_ASSETS } from "../data/assets.js";

export function gcsImage(src, alt, className = "") {
  return `<figure class="gcs-screen ${className}"><img src="${src}" alt="${alt}" /><figcaption>${alt}</figcaption></figure>`;
}

export function gcsCaseSections() {
  return `<div class="case-body gcs-case-body">
    <section class="case-section"><h2>The challenge</h2><div><p>The project required an interface that could make a complex hardware-and-software system understandable to its operator. Flight information, a live camera feed, and mosquito breeding-site detection needed to fit into a clear workflow.</p><p>The challenge was deciding which information deserved immediate attention, how to organize controls around the operator’s tasks, and how the mobile app could support the experience.</p></div></section>
    <section class="case-section"><h2>Mapping the experience</h2><div><p>I mapped the system’s workflows to understand how users would move between flight monitoring, detection information, and flight history. These flows helped define the screen structure, navigation, and placement of key information.</p><p>Separating active monitoring from reviewing past flights also helped establish the responsibilities of the web dashboard and mobile companion app.</p><div class="gcs-gallery gcs-gallery-split">${gcsImage(GCS_ASSETS.webDashboard, "GCS web dashboard showing the main flight monitoring workspace")}${gcsImage(GCS_ASSETS.mobileDashboard, "GCS mobile dashboard showing a focused companion view")}</div></div></section>
    <section class="case-section"><h2>Design decisions</h2><div><div class="design-decision gcs-decision"><div class="gcs-image-frame">${gcsImage(GCS_ASSETS.webStream, "GCS web stream view with camera feed and detection information")}</div><div><h3>Web and mobile, with different jobs</h3><p>The web application serves as the main control and monitoring workspace, while the mobile app focuses on checking flight details and history.</p><p>The larger web interface provides room for the live feed, flight information, and controls to appear together. Mobile prioritizes a focused view of flight records and details that users can access without opening the full dashboard.</p><p>This division gives each platform a clear purpose while maintaining consistent terminology and visual patterns.</p></div></div></div></section>
    <section class="case-section"><h2>The interface</h2><div><p>The main workspace for monitoring flight information and accessing drone controls.</p><div class="gcs-gallery gcs-gallery-web">${gcsImage(GCS_ASSETS.webDashboard, "GCS web dashboard")}${gcsImage(GCS_ASSETS.webStream, "GCS web live camera and detection view")}${gcsImage(GCS_ASSETS.webAnalytics, "GCS web analytics view")}${gcsImage(GCS_ASSETS.webFlightLogs, "GCS web flight logs")}</div><p class="gcs-interface-note">The mobile companion keeps flight information and history available in a focused format.</p><div class="gcs-gallery gcs-gallery-mobile">${gcsImage(GCS_ASSETS.mobileDashboard, "GCS mobile dashboard")}${gcsImage(GCS_ASSETS.mobileAnalytics, "GCS mobile analytics")}${gcsImage(GCS_ASSETS.mobileFlightLogsOne, "GCS mobile flight logs overview")}${gcsImage(GCS_ASSETS.mobileFlightLogsTwo, "GCS mobile flight log details")}</div></div></section>
    <section class="case-section"><h2>What I learned</h2><div><p>This project taught me to approach interface design alongside system behavior. In a connected hardware-and-software project, the usefulness of a screen depends on how clearly it communicates the data and functionality behind it.</p><p>Working across web and mobile also helped me think more carefully about information priority and the purpose of each platform.</p><p>The capstone is complete, but the applications are currently offline. Future improvements would include clearer connection and error states, stronger visual hierarchy, and usability testing to evaluate how easily users understand the monitoring workflow.</p></div></section>
  </div>`;
}
